select models.ModelID,models.modelName, makes.makeName from models
INNER JOIN makes on models.MakeID = makes.MakeID;


select * from cars
select * from makes
select * from models
select * from canProfile

